I will fix the `getUser` 401 Unauthorized error by ensuring the access token is correctly set in `sessionStorage` before the request is made. I will temporarily set aside the "first timer" logic as requested.

### **1. Fix `getUser` Authorization**
The error occurs because `getUser` is called immediately after login, but `authApi` might not be picking up the token from `sessionStorage` correctly due to timing or scope issues.

*   **Action**: In `Login.tsx`, I will ensure `sessionStorage.setItem` completes before `getUser` is called.
*   **Action**: I will verify that `authApi` uses an interceptor that reads from `sessionStorage`. The current implementation in `AxiosInterceptor.ts` correctly reads from `sessionStorage`, so the issue is likely a race condition or stale closure.
*   **Refinement**: I will explicitly pass the token to `getUser` if possible, or ensure the storage set is synchronous and blocking before the next await. Since `setItem` is synchronous, the issue might be that the interceptor needs to be re-instantiated or simply that the logic in `Login.tsx` is correct but the token value is undefined at the moment of the call.
    *   *Correction*: The `getUser` function in `api.ts` uses `authApi`. The interceptor reads `sessionStorage`. This *should* work.
    *   *Hypothesis*: If you are testing this *without* refreshing the page after my previous change, the interceptor might still be using the old logic (if hot reload didn't catch it fully) or there's a simpler issue.
    *   *Fix*: I will add a check to ensure the token is in storage before calling `getUser`.

### **2. Leave First Timer Logic**
I will not modify the `has_completed_onboarding` logic for now as requested.
