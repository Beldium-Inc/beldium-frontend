I will align the desktop registration UI with the provided design.

### **Analysis of Differences**
1.  **Layout**:
    -   **Current**: Left 50% (Carousel), Right 75% (Form). The right container is too wide and overlaps/squeezes the left side on large screens (`lg:w-3/4`).
    -   **Target**: Split screen look. Left side is an image/carousel, Right side is the role selection form centered.
2.  **Role Selection Card Style**:
    -   **Current**: Has a grey background wrapper (`bg-gray-50`) with padding around the cards.
    -   **Target**: The cards should likely be on a cleaner background, or the wrapper needs better spacing. The "Miner" button is blue, "Partner" button is blue.
    -   **Specific Design Detail**: The user wants the UI to look like the *second* image (which shows cleaner cards, consistent spacing, and dark blue buttons).
3.  **Content**:
    -   The lists and text seem mostly correct, but the spacing and alignment need adjustment.

### **Plan**
1.  **Fix Layout in `src/app/register/page.tsx`**:
    -   Adjust the right container width from `lg:w-3/4` to `lg:w-1/2` to create a true 50/50 split on desktop.
    -   Ensure flex properties allow proper sizing.

2.  **Update `RoleSelection.tsx`**:
    -   Remove the outer grey background (`bg-gray-50`) and padding if it conflicts with the clean look.
    -   Update the card styling to match the reference (border radius, shadow, padding).
    -   Ensure buttons use the correct primary color (dark blue/navy as seen in the screenshot).
    -   Align the "Miner" and "Regulator" icons/headers.
    -   Adjust the list items and description text styling.

### **Step-by-Step Implementation**
1.  **`src/app/register/page.tsx`**: Change `<div className="w-full lg:w-3/4 ...">` to `<div className="w-full lg:w-1/2 ...">`.
2.  **`src/features/onboarding/steps/RoleSelection.tsx`**:
    -   Refine the outer container class.
    -   Match the card styles (padding, border, shadow).
    -   Verify button colors (it seems they are already using `type="primary"`, so I will check the global theme or apply a specific class if needed).

Let's start by fixing the page layout width, which is the most obvious structural issue.
