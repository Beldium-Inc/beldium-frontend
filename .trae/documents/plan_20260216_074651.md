I will fix the `authApi` request interceptor to correctly attach the Authorization header, as it seems the current check `if (token && config.headers)` might be failing if `config.headers` is undefined or not properly initialized.

### **1. Fix `AxiosInterceptor.ts`**
-   **Modify Request Interceptor**:
    -   Ensure `config.headers` is initialized if it's missing.
    -   Explicitly set the `Authorization` header without conditional checks on `config.headers` existence, as Axios config objects should always have headers.
    -   Double-check that `sessionStorage.getItem("accessToken")` is retrieving the correct key.

### **2. Verify `Login.tsx`**
-   **Confirm Token Storage**: Ensure the token is being stored with the exact key `"accessToken"` before the redirect/`getUser` call. (This was addressed in the previous turn, but I will double-check).

### **3. Verify `authApi` usage in `api.ts`**
-   **Confirm Instance**: Ensure `getUser` uses `authApi`, which has the interceptors attached. (Confirmed: `export async function getUser() { const { data } = await authApi.get("/user/get_user"); return data; }`).

### **Root Cause Hypothesis**
The issue is likely in `AxiosInterceptor.ts`:
```typescript
const token = sessionStorage.getItem("accessToken");
if (token && config.headers) { // <--- This check might be too strict if config.headers is somehow undefined
  config.headers.Authorization = `Bearer ${token}`;
}
```
I will change it to:
```typescript
const token = sessionStorage.getItem("accessToken");
if (token) {
  config.headers = config.headers || {}; // Ensure headers object exists
  config.headers.Authorization = `Bearer ${token}`;
}
```
This ensures the header is always attached if a token exists.
