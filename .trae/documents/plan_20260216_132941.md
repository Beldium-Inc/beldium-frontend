I will revert the changes to `RoleSelection.tsx` and instead apply its previous styling (gray background wrapper, centered card layout) to the `Onboard.tsx` component, so they look consistent.

### **Understanding the Request**
-   **Undo**: You want `RoleSelection.tsx` back to how it was before my last edit (the "first image" style you mentioned, likely the one with the grey background).
-   **Apply**: You want `Onboard.tsx` (the registration form) to look like that "first image" style too.
-   **Goal**: Both steps should share the same UI wrapper: a gray background container with rounded corners, centered content, and specific padding.

### **Plan**
1.  **Revert `RoleSelection.tsx`**:
    -   Restore the `bg-gray-50 rounded-2xl` wrapper.
    -   Restore the `max-w-6xl` grid layout.
    -   Restore the original card styling (border-gray-200, p-8).
    -   Restore the button styling (remove the specific hex codes if they weren't there before).

2.  **Update `Onboard.tsx`**:
    -   Wrap the form in the same `bg-gray-50 rounded-2xl` container.
    -   Center the logo and add the "Create Account" header text (similar to "How Will You Use Beldium?").
    -   Style the form container to look like a "card" (white background, border, shadow) similar to the role selection cards.
    -   Ensure the layout is centered and has a max-width consistent with the role selection screen (e.g., `max-w-xl mx-auto`).

### **Step-by-Step**
1.  **Revert**: I will use `SearchReplace` to put `RoleSelection.tsx` back to its previous state (based on the tool output from before my changes).
2.  **Apply**: I will wrap the content of `Onboard.tsx` in:
    ```tsx
    <div className="w-full py-12 lg:py-16 flex flex-col gap-8 bg-gray-50 rounded-2xl">
      <div className="flex justify-center">
        <Image ... logo ... />
      </div>
      <div className="text-center">
        <h2 className="text-3xl font-semibold">Create Your Account</h2>
        <p className="text-sm text-gray-500">Enter your details to get started.</p>
      </div>
      <div className="max-w-xl mx-auto px-4 w-full">
        <div className="rounded-2xl border border-gray-200 bg-white p-8 shadow-sm">
           {/* Form goes here */}
        </div>
      </div>
    </div>
    ```
