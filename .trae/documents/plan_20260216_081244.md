The user confirmed "good", acknowledging the verification. I will now wrap up the session since the original request (fix infinite loading on miner onboarding) is complete and the compliance onboarding was verified to be working correctly.

### **Summary of Work**
1.  **Fixed Infinite Loading (Miner Onboarding)**: In `src/app/onboarding/page.tsx`, I fixed a bug where a `return` statement prevented `setBooting(false)` from running when the user profile was null. This ensures new users can see the form.
2.  **Verified Compliance Onboarding**: Checked `src/app/complianceonboarding/page.tsx` and confirmed it uses a `finally` block to handle the loading state, so it is not affected by the same bug.

I am ready to hand off.
