Based on my analysis, the frontend codebase **does not** contain any logic that restricts disposable or temporary email addresses. The email validation strictly checks for format (e.g., `type: "email"` in Ant Design).

### **Why the error occurs**
Since the frontend isn't blocking these emails, the "Invalid code or expired" error you are seeing when using a disposable email is coming from the **backend API**. The backend likely has a validation layer or a third-party service (like SendGrid, Mailgun, or a custom blocklist) that flags these domains and either:
1.  Refuses to send the OTP (causing the user to never receive it).
2.  Invalidates the OTP generation for such domains.
3.  Or the email service provider used by the backend silently drops emails to known disposable domains.

### **The Solution**
Because I only have access to the frontend code (`/Users/bellfast/beldium-frontend`), I cannot modify the backend's email filtering logic directly.

**However, to rule out any obscure frontend validation I might have missed, I will:**
1.  Double-check `src/features/onboarding/Onboard.tsx` (the signup form) one more time to ensure no custom regex is being used that might implicitly filter domains.
2.  If the frontend is truly clear (which it appears to be), the only "fix" I can offer here is to improve the error handling to give the user a clearer message if the backend rejects the email, though the current error "Invalid code" suggests the process *tries* to work but fails at the verification step.

**Conclusion:**
This is a **backend issue**. The restriction on disposable emails is enforced by the server or the email delivery service. You need to ask the backend developer to allow disposable domains or check the email service provider's settings.

**For the frontend task:** I will verify `Onboard.tsx` again to be absolutely 100% sure, but currently, no changes on the frontend can force the backend to accept these emails if the server rejects them.
